activity:
    is_active: # Has top priority
        
    run_until:
        
    seconds_to_shut_down:
        60
    liquidate: # Each line should start with '-' so that it will be parsed as a list; 'all' for liquidate all
        
strategies: # Changing the status, parameters of strategies
    BuyIncreaseSellDecrease:
        on_off: # True = on, False = off
            True
signals:
    
monitoring:
    by_strategy: # 'print' or 'csv'
        csv
    account:
        True
    portfolio:
        True